export async function extract(options: {
  path: string;
  output: string;
  framework: "angular" | "react";
}) {
  // Implementation will go here
  console.log("Extract command running with options:", options);
}
